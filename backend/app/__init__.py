"""Pacote da API do Algorise.

`__version__` é a fonte única da versão do backend (SemVer, bump manual a cada
release). O commit e a data de build vêm do ambiente, preenchidos pelo CI.
"""

__version__ = "0.1.0"
